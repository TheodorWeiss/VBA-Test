let
    Keep =
        Table.SelectColumns(
            q_Match_Wechsler_Drotax,
            {"Name", "Aktionspreis min", "Anbieter", "Kalenderwoche", "% Märkte"}
        ),
    Buffered = Table.Buffer(Keep)
in
    Buffered
