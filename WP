Понял задачу ✅
В Power Query ты не “копируешь лист” как объект, а строишь запрос, который возвращает таблицу, и затем загружаешь результат на лист Multikennzahlen, выбрав “Существующий лист” — Excel при обновлении перезапишет диапазон выгрузки, при этом сам лист не удаляется, значит ссылки/формулы на лист останутся валидными (если они ссылаются на лист/ячейки, а не на “таблицу, которая исчезнет”).

Ниже готовый M-код, который:
	•	берёт последний по дате файл из папки InboxDrop
	•	открывает его
	•	берёт первый лист (или можно указать имя)
	•	возвращает Data этого листа

⸻

M-код (берём первый лист из самого свежего файла)

let
    // 1) Папка, куда VBA сохраняет вложения
    Source = Folder.Files("C:\Users\weissT\Documents\Wechsler\InboxDrop"),

    // 2) (опционально) оставляем только Excel-файлы
    OnlyExcel = Table.SelectRows(Source, each [Extension] = ".xlsx" or [Extension] = ".xlsb" or [Extension] = ".xlsm"),

    // 3) Самый свежий файл
    Sorted = Table.Sort(OnlyExcel, {{"Date modified", Order.Descending}}),
    Latest = Sorted{0}[Content],

    // 4) Открываем книгу
    Wb = Excel.Workbook(Latest, null, true),

    // 5) Берём первый лист (Kind="Sheet")
    SheetsOnly = Table.SelectRows(Wb, each [Kind] = "Sheet"),
    FirstSheetData = SheetsOnly{0}[Data]

in
    FirstSheetData


⸻

Если нужно брать конкретный лист по имени (рекомендую, стабильнее)

Заменяешь блок (5) на:

    SheetName = "Multikennzahlen",         // <-- имя листа в файле-вложении
    SheetData = Wb{[Item=SheetName, Kind="Sheet"]}[Data]
in
    SheetData

(И не забудь убрать/заменить FirstSheetData на SheetData.)

⸻

Как правильно “залить” это на твой лист Multikennzahlen (без удаления листа)
	1.	В Power Query: Close & Load To…
	2.	Выбрать Existing worksheet
	3.	Указать ячейку Multikennzahlen!$A$1
	4.	ОК
	5.	При обновлении Excel будет перезаписывать выгружаемый диапазон, сам лист останется.

⸻

Важное ограничение (чтобы ожидания совпали)

Power Query подтягивает used range листа во вложении.
То есть если данные начинаются не в A1 — это ок (ты сказал “не важно”), но пустые “поля слева/сверху” как в настоящем копировании листа не сохранятся.

⸻

Если скажешь:
	•	точное имя листа во вложении (как в Outlook/Excel),
	•	и нужно ли брать “самый свежий” файл по дате письма или по дате файла (сейчас — по дате файла),

я подстрою код идеально под твой кейс (например, выбрать файл по маске имени, чтобы не схватить случайный xlsx из папки).
