Отлично, Тео. Теперь делаем это строго и правильно, без путаницы.

У тебя есть:
	•	Artikelnummer
	•	CleanName

Нам нужно получить:
	•	TF = сколько раз токен встречается всего
	•	DF = в скольких разных артикулах встречается токен

Это делается в Power Query в два разных шага.

⸻

🧩 ШАГ 1 — Сделать таблицу “Artikel–Token”

1️⃣ Загрузи таблицу в Power Query

Daten → Aus Tabelle/Bereich

2️⃣ Оставь только:
	•	Artikelnummer
	•	CleanName

3️⃣ Разбей CleanName

Spalte teilen → Nach Trennzeichen → Leerzeichen
⚠ Выбери In Zeilen aufteilen (очень важно)

Теперь у тебя:

Artikelnummer | Token

4️⃣ Удали пустые токены

Filter → Token ≠ null
Filter → Token ≠ “”

Назови этот запрос, например:
👉 TokenTable

Закрой и НЕ загружай пока.

⸻

🧮 ШАГ 2 — Считаем TF (общая частота токена)

1️⃣ В Power Query → Rechtsklick на TokenTable → Duplizieren

2️⃣ В новой копии:
	•	Gruppieren nach
	•	Gruppieren nach: Token
	•	Neue Spalte: TF
	•	Operation: Anzahl Zeilen

Получишь:

Token | TF

Это Total Frequency.

Назови запрос:
👉 TF_Table

⸻

🧮 ШАГ 3 — Считаем DF (в скольких артикулах встречается токен)

Вот тут главное отличие.

DF считает:

Сколько разных Artikelnummer содержат токен

1️⃣ Вернись к исходному TokenTable

2️⃣ Удали дубликаты по паре:
	•	Artikelnummer
	•	Token

В PQ:
Start → Duplikate entfernen
Выдели ОБЕ колонки:
	•	Artikelnummer
	•	Token

Теперь каждая пара токен–артикул уникальна.

3️⃣ Теперь Gruppieren nach:
	•	Gruppieren nach: Token
	•	Neue Spalte: DF
	•	Operation: Anzahl Zeilen

Получишь:

Token | DF

Назови:
👉 DF_Table

⸻

🧩 ШАГ 4 — Объединить TF и DF

1️⃣ Открой TF_Table
2️⃣ Start → Abfragen zusammenführen
3️⃣ Соедини с DF_Table по Token
4️⃣ Тип Join: Left Join

5️⃣ Разверни колонку DF

И получишь:

Token | TF | DF


⸻

📊 Что это означает

Метрика	Смысл
TF	сколько всего раз токен встречается
DF	в скольких уникальных артикулах


⸻

🎯 Пример

Если токен milka:
	•	TF = 320
	•	DF = 310

→ бренд, почти в каждом товаре один раз.

Если g:
	•	TF = 20000
	•	DF = 20000

→ бесполезный служебный токен.

Если kokos:
	•	TF = 30
	•	DF = 30

→ вкус.

⸻

⚠ Важная проверка

После удаления дубликатов (Artikelnummer + Token):

Количество строк должно уменьшиться.

Если не уменьшилось — значит у тебя в CleanName нет повторяющихся токенов внутри строки (это нормально).

⸻

🚀 Дальше

После TF+DF можно:
	•	отфильтровать токены с DF > 30% от всех артикулов (стоп-токены)
	•	построить первый ClusterKey
	•	автоматически выделить бренды

⸻

Если хочешь, следующим шагом я покажу:

👉 как автоматически определить “служебные токены” по DF
👉 или как собрать первый ClusterKey без словаря

Ты сейчас на правильном data-science треке, Тео 👌
