=WENNFEHLER(
  WENN(ISTZAHL(--TEIL(A2;FINDEN("x";A2)-1;1));
     --TEIL(
        LINKS(A2;FINDEN("x";A2)-1);
        MAX(WENN(
              ISTZAHL(--TEIL(LINKS(A2;FINDEN("x";A2)-1);ZEILE($1:$10);1))=FALSCH;
              ZEILE($1:$10)
        ))+1;
        10
     );
     1
  );
1)
